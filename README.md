## Run project
1. ```mkdir -p bootstrap/cache```
2. ```composer install```
3. ```php artisan migrate --seed```
4. ```npm install``` (node v22 or higher)
5. ```composer run dev```
6. Run test ```php artisan test  ```
7. Swagger Docs http://127.0.0.1:8000/api/documentation
